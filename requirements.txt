bleak>=1.1.1
python-osc>=1.8.3
matplotlib>=3.9.4
numpy>=2.0.2
scipy
sounddevice
psutil

# Heavy stack (numba/llvmlite); install separately if you need ML feature extraction:
#   pip install -r requirements-librosa.txt
